#!/bin/bash
# Cargo-installed CLI tools. Run this on a new machine after installing Rust:
#   curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh

cargo install tree-sitter-cli
cargo install rumdl

